<section class="hero-section">
  <div class="hero-noise" aria-hidden="true"></div>
  <div class="container hero-grid">
    <div class="hero-copy animate-fade-in-up animate-duration-slower">
      <div class="hero-kicker">
        <span class="kicker-dot" aria-hidden="true"></span>
        <p class="eyebrow">Selección de alto volumen</p>
      </div>
      <div class="hero-text prose prose-xl prose-sonia max-w-none">
        <h1>De 300 CVs a tus 10 mejores candidatos, en minutos.</h1>
        <p>SonIA es el software de cribado de CV con inteligencia artificial para ETT y empresas de facility services. Lee cada currículum, lo puntúa contra los requisitos de tu oferta y te devuelve una shortlist ordenada, con el motivo de cada posición. Tu equipo deja de revisar 150 CVs y revisa 10.</p>
      </div>
      <div class="hero-actions" aria-label="Acciones principales">
        <a class="button button-primary" href="#contacto">Solicitar una demo</a>
        <a class="button button-secondary" href="#como-funciona">Ver cómo funciona</a>
      </div>
    </div>

    <div class="hero-panel animate-blurred-fade-in animate-delay-200 animate-duration-slower" aria-label="Esquema de las capacidades conectadas de SonIA">
      <div class="capability-map">
        <div class="map-canvas">
          <svg class="map-lines" viewBox="0 0 600 410" preserveAspectRatio="none" aria-hidden="true">
            <path class="connection-line" pathLength="1" d="M300 205C260 166 234 98 194 86" />
            <path class="connection-line" pathLength="1" d="M300 205C258 205 224 205 188 205" />
            <path class="connection-line" pathLength="1" d="M300 205C260 244 234 300 194 316" />
            <path class="connection-line" pathLength="1" d="M300 205C340 166 366 98 406 86" />
            <path class="connection-line" pathLength="1" d="M300 205C342 205 376 205 412 205" />
            <path class="connection-line" pathLength="1" d="M300 205C340 244 366 300 406 316" />

            <path class="flow-line" style="--flow-delay: 0s" pathLength="1" d="M300 205C260 166 234 98 194 86" />
            <path class="flow-line" style="--flow-delay: .4s" pathLength="1" d="M300 205C258 205 224 205 188 205" />
            <path class="flow-line" style="--flow-delay: .8s" pathLength="1" d="M300 205C260 244 234 300 194 316" />
            <path class="flow-line" style="--flow-delay: 1.2s" pathLength="1" d="M300 205C340 166 366 98 406 86" />
            <path class="flow-line" style="--flow-delay: 1.6s" pathLength="1" d="M300 205C342 205 376 205 412 205" />
            <path class="flow-line" style="--flow-delay: 2s" pathLength="1" d="M300 205C340 244 366 300 406 316" />
          </svg>

          <div class="sonia-core">
            <span class="core-ring" aria-hidden="true">
              <img src="/logo_sonia_icon.png" width="512" height="512" alt="" />
            </span>
            <strong>SonIA</strong>
            <small>Analiza · Prioriza · Conecta</small>
          </div>

          <article class="branch-node node-offer">
            <span class="branch-icon" aria-hidden="true">
              <svg viewBox="0 0 24 24"><rect x="3" y="7" width="18" height="13" rx="2" /><path d="M8 7V5a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2M3 12h18" /></svg>
            </span>
            <span><strong>Oferta y requisitos</strong><small>Define qué importa</small></span>
          </article>

          <article class="branch-node node-cvs">
            <span class="branch-icon" aria-hidden="true">
              <svg viewBox="0 0 24 24"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z" /><path d="M14 2v6h6M8 13h8M8 17h5" /></svg>
            </span>
            <span><strong>Carga de CVs</strong><small>Incorpora candidatos</small></span>
          </article>

          <article class="branch-node node-analysis">
            <span class="branch-icon" aria-hidden="true">
              <svg viewBox="0 0 24 24"><circle cx="11" cy="11" r="7" /><path d="m20 20-4-4M11 8v6M8 11h6" /></svg>
            </span>
            <span><strong>Análisis semántico</strong><small>Interpreta los perfiles</small></span>
          </article>

          <article class="branch-node node-ranking">
            <span class="branch-icon" aria-hidden="true">
              <svg viewBox="0 0 24 24"><path d="M8 6h13M8 12h13M8 18h13" /><path d="M3 6h.01M3 12h.01M3 18h.01" /></svg>
            </span>
            <span><strong>Ranking explicado</strong><small>Ordena con motivos</small></span>
          </article>

          <article class="branch-node node-shortlist">
            <span class="branch-icon" aria-hidden="true">
              <svg viewBox="0 0 24 24"><path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2" /><circle cx="9" cy="7" r="4" /><path d="m17 11 2 2 4-4" /></svg>
            </span>
            <span><strong>Shortlist</strong><small>Selecciona quién avanza</small></span>
          </article>

          <article class="branch-node node-contact">
            <span class="branch-icon" aria-hidden="true">
              <svg viewBox="0 0 24 24"><path d="M21 15a4 4 0 0 1-4 4H8l-5 3V7a4 4 0 0 1 4-4h10a4 4 0 0 1 4 4z" /><path d="M8 9h8M8 13h5" /></svg>
            </span>
            <span><strong>Contacto y seguimiento</strong><small>Activa el siguiente paso</small></span>
          </article>
        </div>
        <header class="map-title">
          <strong>Todo lo que tu equipo necesita para priorizar y avanzar.</strong>
        </header>
      </div>
    </div>
  </div>
</section>

<style>
  .hero-section {
    overflow: hidden;
    padding: clamp(40px, 6vh, 64px) 0 clamp(48px, 7vh, 72px);
    position: relative;
  }

  .hero-noise {
    background-image:
      linear-gradient(var(--ink-200) 1px, transparent 1px),
      linear-gradient(90deg, var(--ink-200) 1px, transparent 1px);
    background-size: 44px 44px;
    inset: 0;
    mask-image: radial-gradient(circle at 72% 28%, black, transparent 62%);
    opacity: 0.32;
    position: absolute;
    z-index: -1;
  }

  .hero-grid {
    align-items: center;
    display: grid;
    gap: clamp(var(--space-10), 5vw, var(--space-16));
    grid-template-columns: minmax(0, 0.95fr) minmax(410px, 1.05fr);
  }

  .hero-copy,
  .hero-text {
    display: grid;
    gap: var(--space-5);
  }

  .hero-text h1,
  .hero-text p {
    margin: 0;
  }

  .hero-kicker {
    align-items: center;
    display: inline-flex;
    gap: var(--space-3);
  }

  .kicker-dot {
    background: var(--gold-400);
    border: 4px solid #fdefc2;
    border-radius: var(--radius-full);
    height: 18px;
    width: 18px;
  }

  :global(html[data-theme="dark"]) .kicker-dot {
    border-color: rgb(251 195 60 / 24%);
    box-shadow: 0 0 0 1px rgb(251 195 60 / 12%);
  }

  h1 {
    font-size: clamp(2.4rem, 6.1vw, 4.9rem);
    line-height: var(--leading-tight);
    max-width: 820px;
  }

  .hero-text p {
    font-size: clamp(var(--text-base), 2vw, var(--text-md));
    max-width: 680px;
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-3);
    padding-top: var(--space-2);
  }

  .hero-panel {
    position: relative;
  }

  .capability-map {
    background:
      radial-gradient(circle at 88% 10%, rgb(251 195 60 / 42%), transparent 31%),
      radial-gradient(circle at 10% 88%, rgb(105 94 156 / 18%), transparent 34%),
      linear-gradient(145deg, var(--surface) 0%, color-mix(in srgb, var(--gold-400) 14%, var(--surface)) 48%, var(--purple-50) 100%);
    border: 1px solid var(--purple-200);
    border-radius: 32px;
    box-shadow: 0 22px 54px rgb(37 32 56 / 14%), var(--shadow-md);
    isolation: isolate;
    min-height: 500px;
    overflow: hidden;
    padding: var(--space-5);
    position: relative;
  }

  .map-title {
    display: grid;
    gap: var(--space-1);
    justify-items: center;
    position: relative;
    text-align: center;
    z-index: 3;
  }

  .map-title span {
    background: var(--gold-100);
    border: 1px solid rgb(251 195 60 / 72%);
    border-radius: var(--radius-full);
    color: #443c63;
    font-size: 0.625rem;
    font-weight: 800;
    letter-spacing: var(--tracking-wide);
    padding: 3px var(--space-3);
    text-transform: uppercase;
  }

  .map-title strong {
    color: var(--purple-900);
    font-size: var(--text-base);
  }

  .map-canvas {
    height: 410px;
    position: relative;
  }

  .map-lines {
    inset: 0;
    position: absolute;
    z-index: 0;
  }

  .map-lines .connection-line {
    fill: none;
    opacity: 0.82;
    stroke: var(--purple-400);
    stroke-dasharray: 0.045 0.035;
    stroke-linecap: round;
    stroke-width: 1.6;
    vector-effect: non-scaling-stroke;
  }

  .map-lines .flow-line {
    animation: signal-flow 4.8s linear infinite;
    animation-delay: calc(700ms + var(--flow-delay));
    fill: none;
    opacity: 0;
    stroke: var(--gold-400);
    stroke-dasharray: 0.075 0.925;
    stroke-dashoffset: 1;
    stroke-linecap: round;
    stroke-width: 4;
    vector-effect: non-scaling-stroke;
  }

  .sonia-core {
    align-content: center;
    display: grid;
    justify-items: center;
    left: 50%;
    position: absolute;
    top: 50%;
    transform: translate(-50%, -50%);
    z-index: 2;
  }

  .core-ring {
    animation: core-process 4.8s var(--ease) 1.9s infinite;
    align-items: center;
    display: inline-flex;
    height: 112px;
    justify-content: center;
    margin-bottom: var(--space-3);
    position: relative;
    width: 112px;
  }

  .core-ring img {
    animation: logo-arrive 700ms var(--ease) 150ms both;
    border-radius: 24px;
    border: 7px solid var(--surface);
    box-shadow: 0 18px 42px rgb(20 17 31 / 38%);
    height: 112px;
    object-fit: cover;
    width: 112px;
  }

  .sonia-core > strong {
    color: var(--purple-900);
    font-size: var(--text-base);
  }

  .sonia-core > small {
    color: var(--ink-600);
    font-size: 0.625rem;
    font-weight: 700;
  }

  .branch-node {
    --enter-x: 0px;
    --enter-y: 0px;
    align-items: center;
    animation: branch-appear 760ms var(--ease) var(--branch-delay, 280ms) both;
    background: var(--surface);
    border: 1px solid var(--purple-200);
    border-radius: var(--radius-lg);
    box-shadow: 0 10px 24px rgb(53 47 76 / 11%);
    display: grid;
    gap: var(--space-3);
    grid-template-columns: auto minmax(0, 1fr);
    min-height: 72px;
    padding: var(--space-3) 14px;
    position: absolute;
    transition: border-color var(--dur) var(--ease), box-shadow var(--dur) var(--ease), transform var(--dur) var(--ease);
    width: clamp(150px, 32%, 184px);
    z-index: 1;
  }

  .branch-node:hover {
    border-color: var(--gold-400);
    box-shadow: 0 14px 30px rgb(53 47 76 / 16%);
    transform: translateY(-2px);
  }

  .branch-node > span:last-child {
    display: grid;
    gap: 2px;
    min-width: 0;
  }

  .branch-node strong {
    color: var(--ink-800);
    font-size: 0.775rem;
    line-height: 1.3;
  }

  .branch-node small {
    color: var(--fg3);
    font-size: 0.59375rem;
    line-height: 1.35;
  }

  .branch-icon {
    align-items: center;
    background: var(--purple-100);
    border-radius: var(--radius-md);
    color: var(--purple-700);
    display: inline-flex;
    height: 40px;
    justify-content: center;
    width: 40px;
  }

  .branch-icon svg {
    fill: none;
    height: 17px;
    stroke: currentColor;
    stroke-linecap: round;
    stroke-linejoin: round;
    stroke-width: 1.75;
    width: 17px;
  }

  :global(html[data-theme="dark"]) .capability-map {
    background:
      radial-gradient(circle at 88% 10%, rgb(251 195 60 / 12%), transparent 31%),
      radial-gradient(circle at 10% 88%, rgb(105 94 156 / 28%), transparent 34%),
      linear-gradient(145deg, var(--surface) 0%, #302a24 48%, var(--purple-50) 100%);
    border-color: var(--purple-200);
    box-shadow: 0 24px 56px rgb(0 0 0 / 26%);
  }

  :global(html[data-theme="dark"]) .map-title strong,
  :global(html[data-theme="dark"]) .sonia-core > strong {
    color: var(--ink-900);
  }

  :global(html[data-theme="dark"]) .sonia-core > small {
    color: var(--ink-600);
  }

  .node-offer {
    --branch-delay: 320ms;
    --enter-x: 48px;
    --enter-y: 40px;
    left: 0;
    top: 50px;
  }

  .node-cvs {
    --branch-delay: 470ms;
    --enter-x: 62px;
    left: 0;
    top: 50%;
    transform: translateY(-50%);
  }

  .node-analysis {
    --branch-delay: 620ms;
    --enter-x: 48px;
    --enter-y: -40px;
    bottom: 58px;
    left: 0;
  }

  .node-ranking {
    --branch-delay: 320ms;
    --enter-x: -48px;
    --enter-y: 40px;
    right: 0;
    top: 50px;
  }

  .node-shortlist {
    --branch-delay: 470ms;
    --enter-x: -62px;
    right: 0;
    top: 50%;
    transform: translateY(-50%);
  }

  .node-contact {
    --branch-delay: 620ms;
    --enter-x: -48px;
    --enter-y: -40px;
    bottom: 58px;
    right: 0;
  }

  .node-cvs:hover,
  .node-shortlist:hover {
    transform: translateY(calc(-50% - 2px));
  }

  @keyframes signal-flow {
    0% {
      opacity: 0;
      stroke-dashoffset: 1;
    }

    12%,
    74% {
      opacity: 0.9;
    }

    86%,
    100% {
      opacity: 0;
      stroke-dashoffset: 0;
    }
  }

  @keyframes core-process {
    0%,
    24%,
    42%,
    100% {
      scale: 1;
    }

    31% {
      scale: 1.045;
    }
  }

  @keyframes logo-arrive {
    from {
      opacity: 0;
      scale: 0.8;
    }

    to {
      opacity: 1;
      scale: 1;
    }
  }

  @keyframes branch-appear {
    from {
      opacity: 0;
      scale: 0.94;
      translate: var(--enter-x) var(--enter-y);
    }

    to {
      opacity: 1;
      scale: 1;
      translate: 0 0;
    }
  }

  @media (max-width: 940px) {
    .hero-grid {
      grid-template-columns: 1fr;
    }

    .hero-copy {
      max-width: 820px;
    }

    .hero-panel {
      margin-inline: auto;
      max-width: 720px;
      width: 100%;
    }
  }

  @media (min-width: 941px) and (max-height: 800px) {
    .hero-section {
      padding-block: var(--space-8) var(--space-10);
    }

    h1 {
      font-size: clamp(2.4rem, 5.2vw, 4rem);
    }
  }

  @media (max-width: 620px) {
    .capability-map {
      border-radius: var(--radius-xl);
      min-height: 0;
      padding: var(--space-4);
    }

    .map-canvas {
      display: grid;
      gap: var(--space-3);
      grid-template-columns: repeat(2, minmax(0, 1fr));
      height: auto;
      padding-top: var(--space-6);
    }

    .map-lines {
      display: none;
    }

    .sonia-core {
      grid-column: 1 / -1;
      left: auto;
      margin-bottom: var(--space-5);
      position: relative;
      top: auto;
      transform: none;
    }

    .core-ring {
      height: 96px;
      width: 96px;
    }

    .core-ring img {
      height: 96px;
      width: 96px;
    }

    .branch-node,
    .node-offer,
    .node-cvs,
    .node-analysis,
    .node-ranking,
    .node-shortlist,
    .node-contact {
      bottom: auto;
      left: auto;
      position: relative;
      right: auto;
      top: auto;
      transform: none;
      width: auto;
    }

    .node-cvs:hover,
    .node-shortlist:hover {
      transform: translateY(-2px);
    }
  }

  @media (max-width: 420px) {
    .map-canvas {
      grid-template-columns: 1fr;
    }

    .sonia-core {
      grid-column: 1;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .branch-node,
    .core-ring img {
      animation: none;
    }

    .map-lines .flow-line {
      display: none;
    }
  }
</style>
