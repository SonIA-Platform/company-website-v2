<header class="site-header">
  <div class="container header-inner">
    <a class="brand animate-blurred-fade-in animate-duration-slower" href="/" aria-label="SonIA, inicio">
      <span class="brand-logo" aria-hidden="true">
        <img class="logo-icon" src="/logo_sonia_icon.png" width="512" height="512" alt="" />
        <img class="logo-full" src="/logo.png" width="1466" height="638" alt="" />
      </span>
    </a>
    <nav class="nav" aria-label="Principal">
      <a href="#como-funciona">Cómo funciona</a>
      <a href="#beneficios">Beneficios</a>
      <a href="#producto">Producto</a>
    </nav>
    <a class="button button-primary header-cta" href="#contacto">Solicitar una demo</a>
  </div>
</header>

<style>
  .site-header {
    background: rgb(247 248 248 / 92%);
    border-bottom: 1px solid var(--border);
    backdrop-filter: blur(14px);
    box-shadow: 0 1px 0 rgb(255 255 255 / 70%) inset;
    position: sticky;
    top: 0;
    z-index: 10;
  }

  .header-inner {
    align-items: center;
    display: flex;
    gap: var(--space-6);
    justify-content: flex-start;
    min-height: 76px;
  }

  .brand {
    align-items: center;
    display: inline-flex;
    border-radius: var(--radius-md);
  }

  .brand:hover {
    text-decoration: none;
  }

  .brand-logo {
    background: var(--purple-600);
    border-radius: var(--radius-md);
    display: block;
    height: 42px;
    overflow: hidden;
    position: relative;
    transition: width 420ms var(--ease), box-shadow 320ms var(--ease);
    width: 42px;
  }

  .brand-logo img {
    display: block;
    left: 0;
    position: absolute;
    top: 0;
    transition: opacity 300ms var(--ease), transform 420ms var(--ease);
  }

  .logo-icon {
    height: 42px;
    object-fit: cover;
    opacity: 1;
    transform: scale(1);
    width: 42px;
  }

  .logo-full {
    height: 42px;
    opacity: 0;
    transform: translateX(-4px) scale(0.98);
    width: auto;
  }

  .brand:hover .brand-logo,
  .brand:focus-visible .brand-logo {
    box-shadow: var(--shadow-sm);
    width: 98px;
  }

  .brand:hover .logo-icon,
  .brand:focus-visible .logo-icon {
    opacity: 0;
    transform: scale(0.92);
  }

  .brand:hover .logo-full,
  .brand:focus-visible .logo-full {
    opacity: 1;
    transform: translateX(0) scale(1);
    transition-delay: 70ms, 0ms;
  }

  .nav {
    align-items: center;
    display: flex;
    gap: var(--space-5);
    margin-left: var(--space-2);
  }

  .nav a {
    color: var(--fg2);
    font-size: var(--text-sm);
    font-weight: 700;
    position: relative;
    transition: color var(--dur) var(--ease);
  }

  .nav a::after {
    background: var(--gold-400);
    border-radius: var(--radius-full);
    bottom: -8px;
    content: "";
    height: 2px;
    left: 0;
    position: absolute;
    transform: scaleX(0);
    transform-origin: left;
    transition: transform var(--dur) var(--ease);
    width: 100%;
  }

  .nav a:hover {
    color: var(--purple-700);
    text-decoration: none;
  }

  .nav a:hover::after {
    transform: scaleX(1);
  }

  @media (max-width: 760px) {
    .header-inner {
      flex-wrap: wrap;
      gap: var(--space-4);
      padding-block: var(--space-4);
    }

    .nav {
      order: 3;
      overflow-x: auto;
      width: 100%;
    }

    .header-cta {
      margin-left: auto;
      padding-inline: var(--space-4);
    }
  }

  @media (min-width: 761px) {
    .header-cta {
      margin-left: auto;
    }
  }
</style>
