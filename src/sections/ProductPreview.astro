<section class="product-section" id="producto" aria-labelledby="product-title">
  <div class="container">
    <header class="product-heading">
      <div class="prose prose-lg prose-sonia max-w-none">
        <p class="eyebrow not-prose">Anatomía del ranking</p>
        <h2 id="product-title">El peso de cada requisito cambia el resultado.</h2>
      </div>
      <p>SonIA no busca una coincidencia genérica. Relaciona lo que exige la oferta con las señales disponibles en cada CV y deja el resultado preparado para revisión.</p>
    </header>

    <figure class="ranking-anatomy" aria-labelledby="anatomy-title">
      <header class="anatomy-heading">
        <span>Esquema ilustrativo</span>
      </header>

      <div class="anatomy-stage">
        <article class="anatomy-card requirement-card">
          <header>
            <span class="card-icon" aria-hidden="true">
              <svg viewBox="0 0 24 24"><rect x="3" y="7" width="18" height="13" rx="2" /><path d="M8 7V5a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2M3 12h18" /></svg>
            </span>
            <span><small>Oferta de empleo</small><h3>Requisitos y peso</h3></span>
          </header>

          <ul class="factor-list">
            <li>
              <span><strong>Experiencia similar</strong><small>Requisito crítico</small></span>
              <span class="weight weight-critical" aria-label="Peso crítico"><i></i><i></i><i></i><i></i></span>
            </li>
            <li>
              <span><strong>Atención al cliente</strong><small>Importancia alta</small></span>
              <span class="weight weight-high" aria-label="Peso alto"><i></i><i></i><i></i><i></i></span>
            </li>
            <li>
              <span><strong>Disponibilidad</strong><small>Importancia media</small></span>
              <span class="weight weight-medium" aria-label="Peso medio"><i></i><i></i><i></i><i></i></span>
            </li>
          </ul>
        </article>

        <div class="logic-node" aria-label="SonIA relaciona la información">
          <span class="plus-mark" aria-hidden="true">
            <svg viewBox="0 0 24 24"><path d="M12 5v14M5 12h14" /></svg>
          </span>
        </div>

        <article class="anatomy-card evidence-card">
          <header>
            <span class="card-icon" aria-hidden="true">
              <b>CV</b>
            </span>
            <span><small>CV del candidato</small><h3>Señales detectadas</h3></span>
          </header>

          <ul class="signal-list">
            <li class="signal-positive">
              <span class="signal-mark" aria-hidden="true"><svg viewBox="0 0 24 24"><path d="M20 6 9 17l-5-5" /></svg></span>
              <span><strong>Trayectoria relacionada</strong><small>Coincidencia alta</small></span>
            </li>
            <li class="signal-positive">
              <span class="signal-mark" aria-hidden="true"><svg viewBox="0 0 24 24"><path d="M20 6 9 17l-5-5" /></svg></span>
              <span><strong>Experiencia directa</strong><small>Coincidencia alta</small></span>
            </li>
            <li class="signal-review">
              <span class="signal-mark" aria-hidden="true"><svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="9" /><path d="M12 8v5M12 16h.01" /></svg></span>
              <span><strong>Disponibilidad no indicada</strong><small>Pendiente de validar</small></span>
            </li>
          </ul>
        </article>

        <div class="logic-node logic-arrow" aria-hidden="true">
          <span>
            <svg viewBox="0 0 24 24"><path d="M5 12h14M15 8l4 4-4 4" /></svg>
          </span>
        </div>

        <article class="result-card">
          <div class="result-profile">
            <header>
              <span><small>Ranking resultante</small><strong>Perfil A</strong></span>
              <b>#1</b>
            </header>
            <div class="result-score">
              <span><strong>86</strong><small>/100</small></span>
              <em>Encaje alto</em>
            </div>
          </div>

          <p class="result-copy">Los requisitos con más peso tienen más impacto. La información ausente sigue visible para que el equipo pueda revisarla.</p>

          <div class="ranking-context" aria-label="Posiciones siguientes del ranking ilustrativo">
            <span><b>#2</b><strong>Perfil B</strong><em>72</em></span>
            <span><b>#3</b><strong>Perfil C</strong><em>48</em></span>
          </div>
        </article>
      </div>

      <figcaption>
        <span aria-hidden="true">
          <svg viewBox="0 0 24 24"><path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2" /><circle cx="9" cy="7" r="4" /><path d="m17 11 2 2 4-4" /></svg>
        </span>
        <p><strong>SonIA analiza y prioriza.</strong> Tu equipo revisa, selecciona y decide quién avanza.</p>
      </figcaption>
    </figure>
  </div>
</section>

<style>
  .product-section {
    background: var(--paper);
    padding-block: clamp(var(--space-12), 8vw, var(--space-20));
  }

  .product-heading {
    align-items: end;
    display: grid;
    gap: clamp(var(--space-6), 5vw, var(--space-16));
    grid-template-columns: minmax(0, 1.15fr) minmax(280px, 0.85fr);
    margin-bottom: var(--space-8);
  }

  .product-heading :global(.prose) {
    display: grid;
    gap: var(--space-3);
  }

  .product-heading :global(h2),
  .product-heading :global(p) {
    margin: 0;
  }

  .product-heading :global(h2) {
    font-size: clamp(var(--text-xl), 3.8vw, var(--text-3xl));
    line-height: 1.14;
    max-width: 760px;
  }

  .product-heading > p {
    border-left: 3px solid var(--gold-400);
    padding-left: var(--space-5);
  }

  .ranking-anatomy {
    background: var(--ink-50);
    border: 1px solid var(--border);
    border-radius: 20px;
    box-shadow: 0 16px 42px rgb(37 32 56 / 10%);
    margin: 0;
    overflow: hidden;
  }

  .anatomy-heading {
    align-items: center;
    background: var(--surface);
    border-bottom: 1px solid var(--border);
    display: flex;
    gap: var(--space-4);
    justify-content: space-between;
    padding: var(--space-4) var(--space-5);
  }

  .anatomy-heading > span {
    background: var(--purple-100);
    border-radius: var(--radius-full);
    color: var(--purple-700);
    font-size: 0.625rem;
    font-weight: 800;
    letter-spacing: var(--tracking-wide);
    padding: var(--space-1) var(--space-3);
    text-transform: uppercase;
  }

  .anatomy-heading > div {
    display: grid;
    gap: 2px;
    text-align: right;
  }

  .anatomy-heading small,
  .anatomy-card header small,
  .result-card header small {
    color: var(--fg3);
    font-size: 0.625rem;
    font-weight: 800;
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  .anatomy-heading strong {
    color: var(--ink-800);
    font-size: var(--text-sm);
  }

  .anatomy-stage {
    align-items: stretch;
    display: grid;
    gap: var(--space-3);
    grid-template-columns: minmax(220px, 1fr) 74px minmax(220px, 1fr) 54px minmax(210px, 0.86fr);
    padding: var(--space-6);
  }

  .anatomy-card,
  .result-card {
    border-radius: var(--radius-xl);
    min-width: 0;
  }

  .anatomy-card {
    background: var(--surface);
    border: 1px solid var(--border);
    display: grid;
    grid-template-rows: auto 1fr;
    overflow: hidden;
    position: relative;
    z-index: 1;
  }

  .requirement-card {
    border-top: 3px solid var(--gold-400);
  }

  .evidence-card {
    border-top: 3px solid var(--purple-600);
  }

  .anatomy-card > header {
    align-items: center;
    border-bottom: 1px solid var(--border);
    display: grid;
    gap: var(--space-3);
    grid-template-columns: auto 1fr;
    padding: var(--space-4);
  }

  .anatomy-card header > span:last-child,
  .result-card header > span {
    display: grid;
    gap: 2px;
  }

  .anatomy-card h3 {
    color: var(--ink-900);
    font-size: var(--text-sm);
    margin: 0;
  }

  .anatomy-card header small {
    border-radius: var(--radius-full);
    padding: 2px var(--space-2);
    width: max-content;
  }

  .requirement-card header small {
    background: var(--number-bg);
    color: var(--ink-800);
  }

  .evidence-card header small {
    background: var(--purple-100);
    color: var(--purple-700);
  }

  .card-icon {
    align-items: center;
    background: var(--purple-100);
    border-radius: var(--radius-md);
    color: var(--purple-700);
    display: inline-flex;
    height: 38px;
    justify-content: center;
    width: 38px;
  }

  .requirement-card .card-icon {
    background: var(--number-bg);
    color: var(--ink-800);
  }

  .card-icon b {
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    letter-spacing: -0.04em;
  }

  .card-icon svg,
  .signal-mark svg,
  figcaption svg {
    fill: none;
    height: 18px;
    stroke: currentColor;
    stroke-linecap: round;
    stroke-linejoin: round;
    stroke-width: 1.8;
    width: 18px;
  }

  .factor-list,
  .signal-list {
    display: grid;
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .factor-list li,
  .signal-list li {
    align-items: center;
    display: grid;
    gap: var(--space-3);
    min-height: 78px;
    padding: var(--space-3) var(--space-4);
  }

  .factor-list li {
    grid-template-columns: 1fr auto;
  }

  .signal-list li {
    grid-template-columns: auto 1fr;
  }

  .factor-list li + li,
  .signal-list li + li {
    border-top: 1px solid var(--border);
  }

  .factor-list li > span:first-child,
  .signal-list li > span:last-child {
    display: grid;
    gap: 2px;
  }

  .factor-list strong,
  .signal-list strong {
    color: var(--ink-800);
    font-size: var(--text-xs);
  }

  .factor-list small,
  .signal-list small {
    color: var(--fg3);
    font-size: 0.625rem;
  }

  .weight {
    display: flex;
    gap: 3px;
  }

  .weight i {
    background: var(--ink-200);
    border-radius: var(--radius-full);
    height: 18px;
    width: 5px;
  }

  .weight-critical i,
  .weight-high i:nth-child(-n + 3),
  .weight-medium i:nth-child(-n + 2) {
    background: var(--purple-600);
  }

  .signal-mark {
    align-items: center;
    border-radius: var(--radius-full);
    display: inline-flex;
    height: 32px;
    justify-content: center;
    width: 32px;
  }

  .signal-mark svg {
    height: 16px;
    width: 16px;
  }

  .signal-positive .signal-mark {
    background: var(--match-strong-bg);
    color: var(--match-strong);
  }

  .signal-review .signal-mark {
    background: var(--match-mid-bg);
    color: var(--match-mid);
  }

  .logic-node {
    align-content: center;
    display: grid;
    gap: var(--space-2);
    justify-items: center;
    text-align: center;
  }

  .logic-node > span {
    align-items: center;
    background: var(--number-bg);
    border: 1px solid var(--gold-400);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-md);
    display: inline-flex;
    height: 52px;
    justify-content: center;
    overflow: hidden;
    width: 52px;
  }

  .logic-node svg {
    fill: none;
    height: 20px;
    stroke: #564b7f;
    stroke-linecap: round;
    stroke-linejoin: round;
    stroke-width: 1.8;
    width: 20px;
  }

  .logic-node:not(.logic-arrow) > span {
    background: transparent;
    border: 0;
    border-radius: 0;
    box-shadow: none;
    color: var(--gold-400);
    height: 64px;
    overflow: visible;
    width: 64px;
  }

  .plus-mark svg {
    height: 38px;
    stroke: var(--gold-400);
    stroke-width: 2;
    width: 38px;
  }

  .logic-node small {
    color: var(--fg3);
    font-size: 0.5625rem;
    font-weight: 800;
    text-transform: uppercase;
  }

  .logic-arrow > span {
    background: transparent;
    border: 0;
    border-radius: 0;
    box-shadow: none;
    height: 64px;
    overflow: visible;
    width: 64px;
  }

  .logic-arrow svg {
    height: 48px;
    stroke: var(--gold-400);
    stroke-width: 2;
    width: 48px;
  }

  .result-card {
    background: var(--purple-900);
    border-top: 3px solid var(--gold-400);
    color: var(--fg-on-brand);
    display: grid;
    gap: var(--space-4);
    padding: var(--space-5);
  }

  .result-profile {
    display: grid;
    gap: var(--space-4);
  }

  .result-profile > header {
    align-items: start;
    display: flex;
    gap: var(--space-3);
    justify-content: space-between;
  }

  .result-profile header small {
    color: rgb(244 245 250 / 58%);
  }

  .result-profile header strong {
    color: var(--fg-on-brand);
    font-size: var(--text-md);
  }

  .result-profile header > b {
    color: var(--gold-400);
    font-family: var(--font-mono);
    font-size: var(--text-sm);
  }

  .result-score {
    align-items: end;
    display: flex;
    gap: var(--space-3);
    justify-content: space-between;
  }

  .result-score > span {
    align-items: baseline;
    display: flex;
  }

  .result-score strong {
    color: var(--fg-on-brand);
    font-family: var(--font-mono);
    font-size: 3.4rem;
    line-height: 0.9;
  }

  .result-score small {
    color: rgb(244 245 250 / 58%);
    font-family: var(--font-mono);
    font-size: 0.625rem;
  }

  .result-score em {
    background: var(--match-strong-bg);
    border-radius: var(--radius-full);
    color: var(--match-strong);
    font-size: 0.625rem;
    font-style: normal;
    font-weight: 800;
    padding: var(--space-1) var(--space-3);
    white-space: nowrap;
  }

  .result-copy {
    color: rgb(244 245 250 / 72%);
    font-size: var(--text-xs);
    line-height: 1.55;
  }

  .ranking-context {
    align-self: end;
    border-top: 1px solid rgb(255 255 255 / 14%);
    display: grid;
    padding-top: var(--space-3);
  }

  .ranking-context > span {
    align-items: center;
    display: grid;
    gap: var(--space-2);
    grid-template-columns: auto 1fr auto;
    padding-block: var(--space-2);
  }

  .ranking-context > span + span {
    border-top: 1px solid rgb(255 255 255 / 10%);
  }

  .ranking-context b,
  .ranking-context em {
    color: var(--gold-400);
    font-family: var(--font-mono);
    font-size: 0.625rem;
    font-style: normal;
  }

  .ranking-context strong {
    color: var(--fg-on-brand);
    font-size: var(--text-xs);
  }

  figcaption {
    align-items: center;
    background: var(--surface);
    border-top: 1px solid var(--border);
    display: flex;
    gap: var(--space-3);
    justify-content: center;
    padding: var(--space-4);
  }

  figcaption > span {
    color: var(--purple-600);
    display: inline-flex;
  }

  figcaption p,
  figcaption strong {
    color: var(--purple-800);
    font-size: var(--text-sm);
  }

  @media (max-width: 1040px) {
    .anatomy-stage {
      grid-template-columns: minmax(220px, 1fr) 74px minmax(220px, 1fr);
    }

    .result-card {
      grid-column: 1 / -1;
    }

    .logic-arrow {
      display: none;
    }
  }

  @media (max-width: 820px) {
    .product-heading {
      align-items: start;
      grid-template-columns: 1fr;
    }

    .product-heading > p {
      max-width: 680px;
    }
  }

  @media (max-width: 680px) {
    .ranking-anatomy {
      border-radius: var(--radius-xl);
    }

    .anatomy-heading {
      align-items: flex-start;
      flex-direction: column;
    }

    .anatomy-heading > div {
      text-align: left;
    }

    .anatomy-stage {
      grid-template-columns: 1fr;
      padding: var(--space-4);
    }

    .logic-node {
      min-height: 78px;
    }

    .result-card {
      grid-column: auto;
      grid-template-columns: 1fr;
    }

    .logic-arrow {
      display: grid;
    }

    figcaption {
      align-items: flex-start;
      justify-content: flex-start;
    }
  }
</style>
