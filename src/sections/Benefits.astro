<section class="benefits" id="beneficios" aria-labelledby="benefits-title">
  <div class="container">
    <header class="benefits-heading animate-fade-in-up timeline-view animate-range-[entry_0%_cover_25%]">
      <div class="prose prose-lg prose-invert max-w-none">
        <p class="eyebrow not-prose">Beneficios operativos</p>
        <h2 id="benefits-title">Por qué SonIA</h2>
      </div>
      <p>El valor no está solo en ordenar candidatos. Está en hacer que el resultado sea comprensible y se convierta en la siguiente acción del equipo.</p>
    </header>

    <div class="benefits-bento animate-fade-in-up timeline-view animate-range-[entry_5%_cover_35%]">
      <article class="benefit benefit-primary">
        <div class="benefit-meta">
          <span class="benefit-number">01</span>
          <span class="benefit-label">Priorización</span>
        </div>
        <div class="benefit-copy prose prose-lg prose-sonia max-w-none">
          <h3>Reduce el prefiltrado repetitivo</h3>
          <p>SonIA organiza el volumen inicial para que el equipo empiece por los perfiles con más señales de encaje.</p>
        </div>
        <div class="screening-visual" aria-hidden="true">
          <div class="cv-list">
            <div class="cv-item cv-item-1">
              <img src="/file.png" width="512" height="512" alt="" />
              <span><i></i><i></i></span>
            </div>
            <div class="cv-item cv-item-2">
              <img src="/file.png" width="512" height="512" alt="" />
              <span><i></i><i></i></span>
            </div>
            <div class="cv-item cv-item-3">
              <img src="/file.png" width="512" height="512" alt="" />
              <span><i></i><i></i></span>
            </div>
            <div class="cv-item cv-item-4">
              <img src="/file.png" width="512" height="512" alt="" />
              <span><i></i><i></i></span>
            </div>
            <div class="cv-item cv-item-5">
              <img src="/file.png" width="512" height="512" alt="" />
              <span><i></i><i></i></span>
            </div>
          </div>
          <div class="processing-node">
            <span class="processing-icon">
              <img src="/logo_sonia_icon.png" width="512" height="512" alt="" />
            </span>
            <span>Analiza</span>
          </div>
          <div class="shortlist-panel">
            <div class="shortlist-heading">
              <strong>Candidatos</strong>
              <span>Demo</span>
            </div>
            <div class="candidate-row">
              <b>#1</b><span><strong>Perfil A</strong><i></i></span><em>Alto</em>
            </div>
            <div class="candidate-row">
              <b>#2</b><span><strong>Perfil B</strong><i></i></span><em>Medio</em>
            </div>
            <div class="candidate-row">
              <b>#3</b><span><strong>Perfil C</strong><i></i></span><em>Revisar</em>
            </div>
          </div>
        </div>
        <p class="benefit-summary">Olvídate de los centenares de CVs sueltos, deja que SonIA se encargue; tú solo tienes que subir los archivos y seleccionar de forma más óptima e inteligente.</p>
        <div class="benefit-placeholder" aria-label="Indicadores operativos de ejemplo">
          <div class="metric">
            <strong>+200</strong>
            <span>CVs</span>
            <small>Por ejecución</small>
          </div>
          <div class="metric">
            <strong>&lt;10</strong>
            <span>min</span>
            <small>Hasta el primer ranking</small>
          </div>
          <div class="metric">
            <strong>↓€</strong>
            <span>coste</span>
            <small>Ahorra y optimiza</small>
          </div>
        </div>
      </article>

      <div class="benefits-side">
        <article class="benefit benefit-explain">
          <div class="benefit-meta">
            <span class="benefit-number">02</span>
            <span class="benefit-label">Transparencia</span>
          </div>
          <div class="benefit-copy prose prose-base prose-sonia max-w-none">
            <h3>Entiende el ranking</h3>
            <p>Visualiza claramente los motivos detrás de cada posición del candidato.</p>
          </div>
          <div class="ranking-card" aria-label="Ejemplo demostrativo de ranking explicado">
            <div class="candidate-summary">
              <span class="candidate-avatar">PA</span>
              <span class="candidate-name"><strong>Perfil A</strong><small>Posición 6 del ranking</small></span>
              <span class="match-label">Encaje alto</span>
            </div>
            <div class="reason-list">
              <div class="reason-row positive">
                <span class="reason-dot" aria-hidden="true"></span>
                <span><strong>Experiencia relevante</strong><small>Señal a favor</small></span>
              </div>
              <div class="reason-row positive">
                <span class="reason-dot" aria-hidden="true"></span>
                <span><strong>Requisitos clave</strong><small>Coincidencia detectada</small></span>
              </div>
              <div class="reason-row review">
                <span class="reason-dot" aria-hidden="true"></span>
                <span><strong>Disponibilidad</strong><small>Pendiente de validar</small></span>
              </div>
            </div>
          </div>
        </article>

        <article class="benefit benefit-contact">
          <div class="benefit-meta">
            <span class="benefit-number">03</span>
            <span class="benefit-label">Continuidad</span>
          </div>
          <div class="benefit-copy prose prose-base prose-sonia max-w-none">
            <h3>Conecta filtrado y seguimiento</h3>
            <p>La lista de candidatos no queda aislada. Contacta de manera automatizada con los candidatos para recoger datos necesarios y concertar entrevistas sin esfuerzo.</p>
          </div>
          <div class="continuity-map" aria-label="Flujo desde el ranking hasta la entrevista">
            <ol>
              <li>
                <span class="map-node" aria-hidden="true">
                  <svg viewBox="0 0 24 24"><path d="M8 6h13M8 12h13M8 18h13" /><path d="M3 6h.01M3 12h.01M3 18h.01" /></svg>
                </span>
                <strong>Ranking</strong>
              </li>
              <li>
                <span class="map-node" aria-hidden="true">
                  <img class="whatsapp-logo" src="/whatsapp.svg" width="24" height="24" alt="" />
                </span>
                <strong>Contacto</strong>
              </li>
              <li>
                <span class="map-node" aria-hidden="true">
                  <svg viewBox="0 0 24 24"><rect x="3" y="5" width="18" height="16" rx="2" /><path d="M16 3v4M8 3v4M3 11h18" /></svg>
                </span>
                <strong>Entrevista</strong>
              </li>
            </ol>
          </div>
        </article>
      </div>
    </div>
  </div>
</section>

<style>
  .benefits {
    background: var(--purple-900);
    color: var(--fg-on-brand);
    padding-block: clamp(var(--space-12), 7vw, var(--space-20));
  }

  .benefits-heading {
    align-items: end;
    display: grid;
    gap: clamp(var(--space-6), 5vw, var(--space-16));
    grid-template-columns: minmax(0, 1.15fr) minmax(280px, 0.85fr);
    margin-bottom: var(--space-8);
  }

  .benefits-heading :global(.prose) {
    display: grid;
    gap: var(--space-3);
  }

  .benefits-heading :global(h2),
  .benefits-heading :global(p) {
    margin: 0;
  }

  .benefits-heading :global(h2) {
    color: var(--fg-on-brand);
    font-size: clamp(var(--text-xl), 3.8vw, var(--text-3xl));
    line-height: 1.14;
    max-width: 760px;
  }

  .benefits-heading .eyebrow {
    color: var(--gold-400);
  }

  .benefits-heading > p {
    border-left: 3px solid var(--gold-400);
    color: rgb(244 245 250 / 76%);
    padding-left: var(--space-5);
  }

  .benefits-bento {
    align-items: stretch;
    display: grid;
    gap: var(--space-4);
    grid-template-columns: minmax(0, 1.35fr) minmax(340px, 0.95fr);
  }

  .benefits-side {
    align-self: stretch;
    display: grid;
    gap: var(--space-4);
    grid-template-rows: repeat(2, minmax(0, 1fr));
    height: 100%;
  }

  .benefit {
    border: 1px solid var(--border);
    border-radius: 22px;
    box-shadow: 0 10px 30px rgb(20 17 31 / 10%);
    display: grid;
    height: 100%;
    overflow: hidden;
    padding: clamp(var(--space-5), 2.4vw, 28px);
  }

  .benefit-primary {
    align-content: start;
    align-self: stretch;
    background: var(--purple-50);
    grid-template-rows: auto auto auto 1fr;
    height: 100%;
    min-height: 0;
  }

  .benefit-explain {
    background: var(--purple-50);
  }

  .benefit-contact {
    background: var(--purple-50);
  }

  .benefit-meta {
    align-items: center;
    display: flex;
    justify-content: space-between;
    margin-bottom: var(--space-5);
  }

  .benefit-meta > .benefit-number {
    align-items: center;
    background: var(--number-bg);
    border-radius: var(--radius-full);
    color: var(--fg-on-gold);
    display: inline-flex;
    font-family: var(--font-mono);
    font-size: var(--text-sm);
    font-weight: 600;
    height: 38px;
    justify-content: center;
    letter-spacing: -0.04em;
    width: 38px;
  }

  .benefit-meta > .benefit-label {
    background: var(--surface);
    border: 1px solid var(--purple-400);
    border-radius: var(--radius-full);
    color: var(--purple-700);
    font-size: 0.6875rem;
    font-weight: 800;
    letter-spacing: var(--tracking-wide);
    padding: var(--space-1) var(--space-3);
    text-transform: uppercase;
  }

  .benefit-copy {
    display: grid;
    gap: var(--space-3);
  }

  .benefit-copy :global(h3),
  .benefit-copy :global(p) {
    margin: 0;
  }

  .benefit-copy :global(h3) {
    font-size: clamp(var(--text-md), 2.2vw, var(--text-xl));
    line-height: var(--leading-snug);
  }

  .benefit-copy :global(p) {
    line-height: 1.65;
  }

  .screening-visual {
    align-self: end;
    background: var(--paper);
    border: 1px solid var(--border);
    border-radius: var(--radius-xl);
    display: grid;
    gap: var(--space-5);
    grid-template-columns: minmax(110px, 0.78fr) 72px minmax(190px, 1.22fr);
    margin-top: var(--space-6);
    padding: var(--space-5);
  }

  .cv-list {
    min-height: 220px;
    position: relative;
  }

  .cv-item {
    align-items: center;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius-md);
    box-shadow: var(--shadow-sm);
    display: grid;
    gap: var(--space-3);
    grid-template-columns: auto 1fr;
    padding: var(--space-3);
    position: absolute;
    transition: transform var(--dur) var(--ease);
  }

  .cv-item-1 { left: 1%; top: 0; transform: rotate(-2.5deg); width: 96%; z-index: 2; }
  .cv-item-2 { left: 13%; top: 44px; transform: rotate(2deg); width: 84%; z-index: 4; }
  .cv-item-3 { left: 0; top: 91px; transform: rotate(-1.25deg); width: 92%; z-index: 3; }
  .cv-item-4 { left: 10%; top: 137px; transform: rotate(2.75deg); width: 80%; z-index: 1; }
  .cv-item-5 { left: 2%; top: 174px; transform: rotate(-2deg); width: 91%; z-index: 0; }

  .cv-list:hover .cv-item-1 { transform: translate(-3px, -2px) rotate(-3deg); }
  .cv-list:hover .cv-item-2 { transform: translate(4px, -1px) rotate(2.5deg); }
  .cv-list:hover .cv-item-3 { transform: translate(-2px, 2px) rotate(-1.75deg); }
  .cv-list:hover .cv-item-4 { transform: translate(3px, 3px) rotate(3.25deg); }
  .cv-list:hover .cv-item-5 { transform: translate(-3px, 3px) rotate(-2.5deg); }

  .cv-item img {
    height: 24px;
    object-fit: contain;
    width: 24px;
  }

  :global(html[data-theme="dark"]) .cv-item img {
    filter: invert(1);
  }

  .cv-item > span {
    display: grid;
    gap: 6px;
  }

  .cv-item i {
    background: var(--ink-200);
    border-radius: var(--radius-full);
    display: block;
    height: 5px;
  }

  .cv-item i:last-child {
    width: 66%;
  }

  .processing-node {
    align-content: center;
    display: grid;
    gap: var(--space-2);
    justify-items: center;
    position: relative;
  }

  .processing-node::before,
  .processing-node::after {
    border-top: 1px dashed var(--purple-300);
    content: "";
    position: absolute;
    top: calc(50% - 10px);
    width: var(--space-5);
  }

  .processing-node::before { right: calc(100% - 4px); }
  .processing-node::after { left: calc(100% - 4px); }

  .processing-icon {
    border-radius: var(--radius-lg);
    display: inline-flex;
    height: 56px;
    position: relative;
    width: 56px;
    z-index: 1;
  }

  .processing-icon::before,
  .processing-icon::after {
    animation: sonia-wave 3.2s var(--ease) infinite;
    border: 1px solid var(--purple-300);
    border-radius: var(--radius-xl);
    content: "";
    inset: 0;
    opacity: 0;
    pointer-events: none;
    position: absolute;
    z-index: 0;
  }

  .processing-icon::after {
    animation-delay: 1.6s;
  }

  .processing-icon img {
    border: 5px solid var(--purple-100);
    border-radius: var(--radius-lg);
    height: 56px;
    object-fit: cover;
    position: relative;
    width: 56px;
    z-index: 1;
  }

  @keyframes sonia-wave {
    0% {
      opacity: 0.32;
      transform: scale(0.96);
    }

    75%,
    100% {
      opacity: 0;
      transform: scale(1.38);
    }
  }

  .processing-node span {
    color: var(--purple-700);
    font-size: 0.6875rem;
    font-weight: 800;
    text-transform: uppercase;
  }

  .shortlist-panel {
    background: var(--purple-50);
    border: 1px solid var(--purple-100);
    border-radius: var(--radius-lg);
    display: grid;
    gap: var(--space-2);
    padding: var(--space-3);
  }

  .shortlist-heading {
    align-items: center;
    display: flex;
    justify-content: space-between;
    padding: var(--space-1) var(--space-1) var(--space-2);
  }

  .shortlist-heading strong {
    color: var(--purple-800);
    font-size: var(--text-sm);
  }

  .shortlist-heading span {
    background: var(--gold-100);
    border-radius: var(--radius-full);
    color: var(--fg-on-gold);
    font-size: 0.5625rem;
    font-weight: 800;
    padding: 2px var(--space-2);
    text-transform: uppercase;
  }

  .candidate-row {
    align-items: center;
    background: var(--surface);
    border: 1px solid var(--purple-100);
    border-radius: var(--radius-md);
    display: grid;
    gap: var(--space-2);
    grid-template-columns: auto 1fr auto;
    padding: var(--space-2);
  }

  .candidate-row > b {
    color: var(--purple-600);
    font-family: var(--font-mono);
    font-size: 0.625rem;
  }

  .candidate-row > span {
    display: grid;
    gap: 5px;
  }

  .candidate-row strong {
    color: var(--ink-800);
    font-size: 0.6875rem;
  }

  .candidate-row i {
    background: var(--ink-200);
    border-radius: var(--radius-full);
    display: block;
    height: 4px;
    width: 72%;
  }

  .candidate-row em {
    background: var(--match-strong-bg);
    border-radius: var(--radius-full);
    color: var(--match-strong);
    font-size: 0.5625rem;
    font-style: normal;
    font-weight: 800;
    padding: 3px var(--space-2);
  }

  .candidate-row:nth-child(3) em {
    background: var(--match-mid-bg);
    color: var(--match-mid);
  }

  .candidate-row:nth-child(4) em {
    background: var(--match-low-bg);
    color: var(--match-low);
  }

  .benefit-placeholder {
    align-items: center;
    align-self: stretch;
    display: grid;
    gap: var(--space-4);
    grid-template-columns: repeat(3, minmax(0, 1fr));
    justify-items: center;
    margin-top: var(--space-4);
    text-align: center;
    transform: translateY(-64px);
  }

  .benefit-summary {
    color: var(--fg2);
    font-size: var(--text-sm);
    line-height: 1.65;
    margin: var(--space-4) 0 0;
  }

  .metric {
    align-content: center;
    display: grid;
    gap: var(--space-1);
    justify-items: center;
    min-height: 96px;
    min-width: 0;
    width: 100%;
  }

  .metric + .metric {
    border-left: 1px solid var(--border);
  }

  .metric strong {
    color: var(--purple-700);
    font-family: var(--font-display);
    font-size: clamp(2rem, 4vw, var(--text-3xl));
    font-variant-numeric: tabular-nums;
    letter-spacing: var(--tracking-tight);
    line-height: 1;
  }

  .metric > span {
    color: var(--ink-800);
    font-size: var(--text-base);
    font-weight: 800;
    text-transform: uppercase;
  }

  .metric small {
    color: var(--fg3);
    font-size: 0.6875rem;
    line-height: 1.4;
  }

  .ranking-card {
    align-self: end;
    background: var(--surface);
    border: 1px solid var(--purple-100);
    border-radius: var(--radius-lg);
    display: grid;
    gap: var(--space-3);
    margin-top: var(--space-4);
    padding: var(--space-3);
  }

  .candidate-summary {
    align-items: center;
    display: grid;
    gap: var(--space-3);
    grid-template-columns: auto 1fr auto;
    padding-bottom: var(--space-3);
    border-bottom: 1px solid var(--border);
  }

  .candidate-avatar {
    align-items: center;
    background: var(--purple-100);
    border-radius: var(--radius-full);
    color: var(--purple-700);
    display: inline-flex;
    font-family: var(--font-mono);
    font-size: 0.625rem;
    font-weight: 800;
    height: 34px;
    justify-content: center;
    width: 34px;
  }

  .candidate-name,
  .reason-row > span:last-child {
    display: grid;
    gap: 2px;
  }

  .candidate-name strong,
  .reason-row strong {
    color: var(--ink-800);
    font-size: var(--text-xs);
  }

  .candidate-name small,
  .reason-row small {
    color: var(--fg3);
    font-size: 0.625rem;
  }

  .match-label {
    background: var(--match-strong-bg);
    border-radius: var(--radius-full);
    color: var(--match-strong);
    font-size: var(--text-xs);
    font-weight: 800;
    padding: var(--space-1) var(--space-3);
    white-space: nowrap;
  }

  .reason-list {
    display: grid;
    gap: var(--space-2);
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }

  .reason-row {
    align-items: center;
    border-radius: var(--radius-md);
    display: grid;
    gap: var(--space-2);
    grid-template-columns: auto 1fr;
    padding: var(--space-2);
  }

  .reason-row:hover {
    background: var(--ink-50);
  }

  .reason-dot {
    background: var(--match-strong);
    border-radius: var(--radius-full);
    height: 8px;
    width: 8px;
  }

  .reason-row.review .reason-dot {
    background: var(--match-mid);
  }

  .continuity-map {
    align-self: end;
    position: relative;
    margin-top: var(--space-5);
    padding: var(--space-3) var(--space-2) 0;
    transform: translateY(-10px);
  }

  .continuity-map::before {
    background: var(--purple-200);
    content: "";
    height: 2px;
    left: 16.666%;
    position: absolute;
    right: 16.666%;
    top: 33px;
  }

  .continuity-map ol {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    list-style: none;
    margin: 0;
    padding: 0;
    position: relative;
  }

  .continuity-map li {
    align-items: center;
    display: grid;
    gap: var(--space-2);
    justify-items: center;
  }

  .map-node {
    align-items: center;
    background: var(--surface);
    border: 2px solid var(--purple-300);
    border-radius: var(--radius-full);
    color: var(--purple-700);
    display: inline-flex;
    height: 44px;
    justify-content: center;
    position: relative;
    width: 44px;
    z-index: 1;
  }

  .map-node svg {
    fill: none;
    height: 20px;
    stroke: currentColor;
    stroke-linecap: round;
    stroke-linejoin: round;
    stroke-width: 1.75;
    width: 20px;
  }

  .map-node .whatsapp-logo {
    height: 20px;
    object-fit: contain;
    width: 20px;
  }

  .continuity-map strong {
    color: var(--ink-800);
    font-size: var(--text-xs);
  }

  @media (max-width: 900px) {
    .benefits-heading {
      align-items: start;
      gap: var(--space-6);
      grid-template-columns: 1fr;
    }

    .benefits-heading > p {
      max-width: 680px;
    }

    .benefits-bento {
      grid-template-columns: 1fr;
    }

    .benefits-side {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      grid-template-rows: 1fr;
    }

  }

  @media (max-width: 620px) {
    .benefits-bento {
      grid-template-columns: 1fr;
    }

    .benefits-side {
      grid-template-columns: 1fr;
      grid-template-rows: auto;
    }

    .benefit-primary,
    .benefit-explain,
    .benefit-contact {
      min-height: auto;
    }

    .benefit {
      border-radius: 20px;
    }

    .screening-visual {
      grid-template-columns: 1fr;
    }

    .cv-list {
      min-height: 220px;
    }

    .processing-node {
      padding-block: var(--space-2);
    }

    .processing-node::before,
    .processing-node::after {
      display: none;
    }

    .ranking-card {
      grid-template-columns: 1fr;
    }

    .reason-list {
      grid-template-columns: 1fr;
    }

    .benefit-placeholder {
      gap: var(--space-2);
      grid-template-columns: repeat(3, minmax(0, 1fr));
      transform: translateY(-12px);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .processing-icon::before,
    .processing-icon::after {
      animation: none;
    }
  }
</style>
