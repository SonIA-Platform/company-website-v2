# Design Reference

Internal design documentation copied from the SonIA coding-agent context pack. Do not publish this folder directly and do not import UI kit previews into production.

The production token mapping currently lives in `src/styles/tokens.css`.
